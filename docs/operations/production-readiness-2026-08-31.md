# وضعیت آمادگی بهره‌برداری سامانه معرفت — ۱۴۰۵/۰۶/۰۹

## محدوده نسخه فعلی

- نقش‌های فعال: مدیر، معاون و دانش‌آموز.
- پنل اختصاصی دبیر و پنل اولیا در نسخه بعدی ساخته می‌شوند.
- داده‌های موجود production هنگام این بررسی حذف یا بازنویسی نشده‌اند.

## وضعیت انتشار

- PR شماره ۱۵ در شاخه `main` ادغام شده است.
- تغییر بازیابی امن رمز مدیر نیز در `main` قرار دارد.
- مهاجرت `student_dashboard_and_resume` در production ثبت شده است.
- حساب مدیر اصلی دارای ایمیل تأییدشده، نقش `admin` و وضعیت فعال است.

## آزمون‌های اجراشده

- ۵۰ آزمون Node برای مسیرهای مدیر، دانش‌آموز، داشبورد، ورود، بازیابی رمز و چرخه آزمون: موفق.
- ۱۸ سناریوی زمان‌سنج، ذخیره پاسخ، تحویل، نتیجه مخفی و بازکردن مجدد آزمون: موفق.
- ۲۶ اسکریپت درون‌صفحه‌ای: از نظر نحوی معتبر.
- آزمون واردسازی گروهی دانش‌آموز و فایل Excel: موفق.
- آزمون ایمنی دارایی‌های منتشرشده: ۳ آزمون موفق.
- آزمون انتخاب چند سؤال از بانک سؤال: موفق.

## کنترل یکپارچگی production

کنترل‌های کد دانش‌آموزی تکراری، روابط تلاش/آزمون/دانش‌آموز، پاسخ‌های یتیم،
زمان تحویل، بازه درصد و تعلق پاسخ به آزمون اجرا شدند و خطای یکپارچگی پیدا نشد.
آمار داخلی production در مخزن عمومی ثبت نمی‌شود.

## داده‌هایی که هنوز باید تکمیل شوند

- برای بخشی از دانش‌آموزان هنوز رمز ورود و کلاس ثبت نشده است.
- حساب واقعی معاون باید پس از دریافت اطلاعات قطعی ایجاد شود.

ورود داده واقعی فقط از مسیر واردسازی گروهی موجود انجام می‌شود. ابتدا فایل در حالت پیش‌نمایش و اعتبارسنجی بررسی می‌شود و فقط ردیف‌های معتبر ثبت می‌شوند. تشابه نام کارکنان به‌تنهایی معیار ادغام دو شخص نیست و هر شخص باید شناسه مستقل داشته باشد.

## وضعیت امنیت

- سیاست آزمایشی یا سیاست ناشناس با نام test/anon روی جدول‌های عمومی پیدا نشد.
- جدول‌های خصوصی رمز و نشست، خواندن مستقیم برای مرورگر ندارند.
- توابع دانش‌آموز با نشست مبهم و کنترل مالکیت کار می‌کنند.
- هشدارهای اجرای ناشناس توابع ورود دانش‌آموز عمدی هستند، زیرا ورود دانش‌آموز پیش از Supabase Auth انجام می‌شود.
- پاسخ صحیح فقط پس از بسته‌شدن آزمون و مجازشدن نمایش جزئیات قابل دریافت است.

## کارهای وابسته به کاربر

1. مدیر باید لینک بازیابی ایمیل را باز کند و رمز جدید شخصی خود را تعیین کند.
2. فایل برنامه سال تحصیلی باید دوباره در دسترس دانلود قرار گیرد یا مجدداً بارگذاری شود؛ نسخه ثبت‌شده فعلی هنگام انتقال خطای موقت منبع فایل می‌دهد.
3. پس از ورود اطلاعات واقعی، یک آزمون محدود با چند دانش‌آموز واقعی اجرا شود.

## معیار شروع بهره‌برداری

- ورود موفق مدیر با رمز جدید
- تکمیل کلاس و رمز همه دانش‌آموزان فعال
- اجرای موفق یک آزمون محدود
- تهیه نسخه پشتیبان پیش از ورود انبوه و ثبت نتیجه یک بازیابی آزمایشی موفق
- تأیید نمایش صحیح در موبایل و رایانه

## شواهد قابل تکرار

- زمان بررسی: ۱۴۰۵/۰۶/۰۹
- نسخه مبنا: commit تغییرناپذیر `ba16f8c9a095b7b720811f390e31cda305b4bd8f`
- آزمون‌های مرورگر و چرخه آزمون: `node --test tests/*.cjs`
- آزمون انتخاب بانک سؤال: `node tests/question-manager-selection.js`
- آزمون دارایی‌های انتشار: `python3 tests/deployment-safety.py`
- کنترل دیتابیس: `docs/operations/production-integrity-check.sql` با دسترسی فقط‌خواندنی

این گزارش وضعیت همان زمان را ثبت می‌کند و جایگزین بررسی تازه پیش از هر انتشار نیست.

## الحاقیه پاسخ تشریحی — ۱۴۰۵/۰۶/۱۰

- Migrationهای پاسخ تشریحی فقط روی پروژه آزمایشی `marefat-school-test` اجرا شده‌اند؛ production تغییری نکرده است.
- قرارداد واقعی PostgreSQL با اجرای تراکنشی و rollback کنترل شد.
- چرخه پذیرش staging شامل سؤال تستی و تشریحی، ذخیره پاسخ، ثبت `pending_manual`، پنهان‌ماندن درصد، تصحیح staff و محاسبه نتیجه نهایی ۱۰۰٪ با موفقیت اجرا شد.
- داده‌های پذیرش rollback شدند: attempt و پاسخ آزمایشی و session اختصاصی باقی نماند و تابع اصلی تشخیص staff بازگردانده شد.
- دو ناسازگاری در staging پیدا و با Migration تکمیلی اصلاح شدند: نبود ستون `question_order` در پاسخ‌ها و حذف‌شدن امتیاز تستی هنگام باز‌محاسبه پس از تصحیح تشریحی.
- هشدار advisor برای RPCهای دانش‌آموز session-aware عمدی است؛ هر RPC توکن هش‌شده و مالکیت attempt را داخل تابع کنترل می‌کند. RPCهای تصحیح مدیر برای `anon` مجاز نیستند و کنترل `v5_is_staff()` دارند.
- انتشار frontend و Migrationهای production تا ادغام PR و اجازه جداگانه انتشار انجام نمی‌شود.
