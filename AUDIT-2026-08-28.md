# بررسی سامانه آزمون معرفت — ۲۸ اوت ۲۰۲۶

> به‌روزرسانی پس از تأیید کاربر: پایان زمان، هماهنگی قفل ذخیره/تصحیح و زمان‌بندی خودکار اصلاح و در سرور فعال شد. جزئیات در [DEADLINE-FINALIZATION.md](DEADLINE-FINALIZATION.md). متن زیر گزارش پایه پیش از این اصلاحات است.

## دامنه و محدودیت شواهد

مبنای بررسی: شاخه `main` در commit `d23878b21145e98e43d210e78a4603282278e797`، ساختار و توابع فعلی Supabase، سیاست‌های دسترسی، سوابق قابل بازیابی و مرورگر زنده.
متن کامل چهار گفتگو بازیابی نشد. خلاصه سوابق جای خواندن تمام گفتگوها را نمی‌گیرد؛ شماره مراحل قبلی نیز بدون متن مصوب، معیار اثبات تکمیل نیست.

## قرار بود چه ساخته شود و وضعیت امروز چیست؟

| بخش | شواهد موجود | وضعیت |
|---|---|---|
| ورود دانش‌آموز با لینک و کد | index.html، exam-access.html و v5_start_exam | پیاده‌سازی شده؛ صفحه زنده باز شد |
| دریافت سؤال و گزینه بدون کلید پاسخ | v5_get_exam_questions و exam.html | پیاده‌سازی شده؛ خواندن تابع تست شد |
| ذخیره و بازیابی پاسخ | v5_save_answer، v5_get_attempt_state | پیاده‌سازی شده؛ خطای ترتیب ذخیره در فرانت‌اند اصلاح شد |
| تایمر و جلوگیری از پاسخ دیرهنگام | v5_check_attempt_time و exam.html | وجود دارد؛ انقضا فعلاً به تصحیح نهایی منتهی نمی‌شود |
| تصحیح سمت سرور و کارنامه | v5_submit_attempt، student-result.html، report.html | تست تراکنشی تصحیح موفق؛ تست کامل مرورگری باقی است |
| ورود و دسترسی کارکنان | Supabase Auth و v5_profiles | هدایت کاربر بدون نشست تست شد؛ ورود مدیر هنوز تست نشده |
| بانک سؤال، ساخت و انتشار | صفحات بانک، سازنده و توابع مدیریتی | کد موجود؛ تست عملی با نشست مدیر باقی است |
| لینک خودکار آزمون | تریگر انتشار در دیتابیس | موجود؛ منطق ساخت و تنظیم لینک نیاز به تست مدیریتی دارد |
| گزارش و تحلیل آموزشی | دو مجموعه صفحات مدیریتی و توابع گزارش | موجود؛ آمار پنل admin-analytics اصلاح شد |
| React + Vite | در سوابق به‌عنوان پیشنهاد آمده | در شاخه فعلی وجود ندارد؛ پروژه HTML/CSS/JavaScript است |
| انتشار و نگهداری | GitHub و Cloudflare Workers | صفحه اصلی Cloudflare در مرورگر پاسخ داد |
| نصب مجدد دیتابیس از مخزن | supabase.sql قدیمی و دو migration | کامل نیست؛ SQL قدیمی برای بازسازی V5 کافی نیست |

## ابزارها و دسترسی‌های بررسی‌شده

| ابزار | نتیجه |
|---|---|
| سوابق گفتگو | بازیابی بخشی از سابقه؛ نه متن کامل چهار چت |
| فایل‌ها | نسخه‌های قدیمی HTML پیدا شد؛ مخزن به‌عنوان مبنای فعلی انتخاب شد |
| GitHub | مخزن و شاخه‌ها خوانده و مخزن دریافت شد |
| Supabase | پروژه فعال؛ خواندن ساختار، توابع، سیاست‌ها و هشدارها موفق |
| اجرای کد | Node و Python فعال؛ تست‌ها اجرا شد |
| مرورگر | Cloudflare باز شد؛ پنل بدون نشست به ورود هدایت شد |
| پیش‌نمایش محلی مرورگر | اتصال به 127.0.0.1 توسط محیط مرورگر مسدود شد |
| انتشار Cloudflare | مستندات و صفحه زنده بررسی شد؛ ابزار مستقیم مدیریت Cloudflare در ابزارهای فعلی پیدا نشد |
| Netlify | ابزار موجود است؛ مسیر پشتیبان تغییر نکرد و تست نشده است |

## وضعیت داده‌ها

۶ آزمون، ۲۱ سؤال اجرایی، ۸ سؤال بانک، ۱۱ دانش‌آموز، ۱۷ نوبت شرکت.
برای همه جدول‌های V5، RLS فعال بود. در نتایج submitted ناسازگاری بین مجموع صحیح/غلط/نزده و تعداد سؤال پیدا نشد. برخورد کد دانش‌آموز پس از تبدیل به حروف کوچک و حذف فاصله نیز پیدا نشد.

## اصلاحات این شاخه

1. ثبت نهایی منتظر پایان صف ذخیره پاسخ‌ها می‌ماند.
2. تغییر سریع گزینه‌ها به ترتیب انتخاب به سرور ارسال می‌شود.
3. ذخیره ناموفق، ثبت نهایی دستی را متوقف می‌کند و خطا به کاربر نشان داده می‌شود.
4. کد استاندارد دانش‌آموز از پاسخ سرور برای درخواست سؤال و وضعیت استفاده می‌شود؛ ورود با حروف کوچک دیگر در این مسیر به کد ناسازگار تبدیل نمی‌شود. خود توابع دیتابیس تغییر نکردند.
5. لینک انتشار مسیر زیرپوشه را حفظ می‌کند؛ مناسب استقرار زیر مسیر GitHub Pages و ریشه Cloudflare.
6. آمار تحلیل از خلاصه نوبت‌های شرکت سرور خوانده می‌شود، نه تعداد دانش‌آموزان و میانگینِ میانگین‌های آنان؛ حداقل و حداکثر نیز از آمار آزمون‌ها گرفته می‌شود.

هر شش تست ابتدا روی کد قبل از اصلاح شکست خورد و پس از اصلاح پاس شد. تست قدیمی انتخاب شناسه سؤال نیز پاس شد، ولی تنها تبدیل آرایه را آزمایش می‌کند و تست واقعی صفحه مدیریت نیست.

## تست‌های انجام‌شده

- بررسی نحو JavaScript داخلی هر ۲۱ صفحه HTML: موفق، قبل و بعد از تغییرات.
- `node --test tests/exam-flow.test.js`: شش تست رفتاری روی کد واقعی صفحات با جایگزینی مرز DOM/RPC؛ تست مرورگری یکپارچه نیست.
- `node tests/question-manager-selection.js`: موفق.
- `git diff --check`: موفق.
- مرورگر زنده: بازشدن صفحه اصلی، رد ورودی خالی، هدایت پنل بدون نشست به صفحه ورود.
- تست توابع واقعی دیتابیس داخل تراکنش برگشت‌پذیر: دریافت سؤال، ذخیره، رد دانش‌آموز نادرست، ارسال، جلوگیری از ذخیره پس از ارسال و ارسال مجدد.
- نمونه تصحیح: ۱ صحیح، ۰ غلط، ۵ نزده، ۱۶٫۶۷ درصد برای آزمون شش‌سؤالی؛ نتیجه مورد انتظار بود.
- همان تست، خطای `STUDENT_ACCESS_DENIED` برای کد با حروف کوچک را در تابع دریافت سؤال تأیید کرد.
- تراکنش rollback شد و تعداد attempts دوباره ۱۷ تأیید شد. داده دائمی و تعریف تابع تغییر نکرد.

## موارد باز؛ ادعای رفع کامل نداریم

### اولویت بالا

- **پایان زمان:** تایمر مرورگر در لحظه پایان submit می‌فرستد، ولی تابع سرور در همان مرز آن را expired می‌کند و تصحیح نمی‌کند. باید سیاست صریح «ثبت و تصحیح خودکار پاسخ‌های قبلاً ذخیره‌شده» یا «انقضا بدون نمره» تعیین شود.
- **هم‌زمانی سمت سرور:** submit ردیف attempt را قفل می‌کند، ولی save_answer و save_answers قفل هم‌ارز ندارند. صف فرانت‌اند مشکل همان صفحه را کاهش می‌دهد؛ چند تب/کلاینت و رقابت درخواست‌ها هنوز نیازمند تست و اصلاح سرور است.
- **ورود کد:** دریافت سؤال و وضعیت هنوز در دیتابیس تطبیق حساس به حروف دارند؛ اصلاح فرانت‌اند جای یکسان‌سازی تمام APIها را نمی‌گیرد.
- **امنیت ورود دانش‌آموز:** لینک + کد دانش‌آموز جای احراز هویت قوی را نمی‌گیرد. قبل از استفاده عمومی، محدودسازی تلاش و مدل دسترسی مدرسه بررسی شود.
- **سیاست‌های قدیمی:** چند جدول legacy دسترسی گسترده authenticated دارند؛ exam_results درج anon دارد. آن‌ها بدون تعیین وابستگی نسخه قدیمی حذف یا بسته نشدند.
- **Snapshot سؤال:** v5_staff_build_exam برای bank_question_id موجود، همان سؤال اجرایی را استفاده می‌کند و متن/گزینه جدید بانک را الزاماً کپی نمی‌کند. ادعای Snapshot مستقل هر آزمون نیازمند اصلاح طراحی و تست است.

### اولویت بعدی

- دو مسیر مدیریتی `admin.html` و `admin-panel.html` با سازنده، گزارش و سطح نقش متفاوت وجود دارد؛ باید مسیر اصلی مشخص و لینک‌ها یکپارچه شوند.
- فیلترکردن بانک در سازنده می‌تواند انتخاب‌های قبلی را با بازسازی DOM پاک کند؛ نیاز به ذخیره انتخاب مستقل از نمایش دارد.
- صفحه exam-questions-v3 شناسه v5_questions را به تابعی با آرگومان شناسه بانک می‌فرستد؛ مسیر قدیمی باید بازنشسته یا اصلاح شود.
- پنل admin-panel میانگین تمام attempts، حتی started را محاسبه می‌کند؛ باید فقط submitted را لحاظ کند.
- تنظیمات randomize_questions و randomize_options در داده وجود دارند، اما مسیر فعلی دریافت سؤال ترتیب ثابت برمی‌گرداند.
- نتیجه بعد از refresh صفحه نهایی به آسانی بازیابی نمی‌شود؛ شناسه attempt باید به شکل امن و قابل استفاده حفظ شود.
- score صفر در فرم بانک با `||1` به یک تبدیل می‌شود؛ نیاز به اعتبارسنجی صریح دارد.
- وابستگی Supabase از CDN با نسخه شناور @2 بارگذاری می‌شود؛ نسخه ثابت و مسیر خطای بارگذاری نیازمند تنظیم است.
- wrangler.jsonc به Pages اشاره دارد ولی مستندات تولید Workers را معرفی می‌کند؛ پیش از انتشار بعدی باید فرمان و فایل تنظیمات واقعی تطبیق داده شود.
- مستندات نصب و migrationها با دیتابیس زنده همگام نیستند؛ نصب مجدد از صفر هنوز قابل اتکا نیست.

## هشدارهای امنیتی

اسکن: ۳ مورد RLS بدون policy، ۹ مورد تابع SECURITY DEFINER قابل فراخوانی توسط anon، ۵۸ مورد قابل فراخوانی توسط authenticated و یک هشدار حفاظت رمزهای افشاشده.
تعداد هشدار معادل تعداد آسیب‌پذیری اثبات‌شده نیست؛ بسیاری از RPCهای عمومی برای ورود با لینک طراحی شده‌اند و توابع کارکنان کنترل نقش دارند. توابع داخلی finalize، check_attempt_time و recalculate در بررسی مجوز برای anon و authenticated قابل اجرا نبودند.

مرجع بررسی: https://supabase.com/docs/guides/database/database-linter

## ادامه تست مشترک

ابتدا انتشار اصلاحات محدود این شاخه و سپس یک آزمون با دانش‌آموز کاملاً آزمایشی: انتخاب، تغییر سریع گزینه، قطع/وصل شبکه، refresh، ثبت نهایی و تطبیق کارنامه. ورود مدیر باید توسط صاحب حساب انجام شود؛ رمز در چت ارسال نشود. تغییرات سیاست انقضا، امنیت و ساختار بانک قبل از انتشار عمومی جداگانه تأیید شوند.
